"""Crypto funding/basis incremental-information pilot.

Research only. Nothing in this package places, cancels, replaces or
simulates an order, touches a broker endpoint, or reads a credential.
Every network call it makes is an unauthenticated HTTPS GET against the
perpetual venue's public historical-dump CDN.
"""
